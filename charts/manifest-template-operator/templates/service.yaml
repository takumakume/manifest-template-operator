apiVersion: v1
kind: Service
metadata:
  name: {{ include "manifest-template-operator.fullname" . }}
  labels:
    {{- include "manifest-template-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manifest-template-operator.selectorLabels" . | nindent 4 }}
